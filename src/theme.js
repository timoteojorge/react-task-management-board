import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#838c91',
        },
        secondary: {
            main: '#FFFFFF',
        },
        background: {
            default: '#838c91',
        },
    },
});


export default theme;